spring:
  application:
    name: microsevice-config-client
  cloud:
    config:
      profile: prod
      uri: http://localhost:8888/
      label: master
server:
  port: 8801
management:
  endpoints:
    web:
      exposure:
        include: "*"